import Image from "next/image";
"use client";

const CustomButton = () => {
  return (
    <button 
    disabled={false}
    type={"button"}
    className={`custom-btn`}
    onClick={() => {}}
    ></button>
  )
}

export default CustomButton;